name: CentralLogin
version: "1.0.0"
main: com.yourpackage.CentralLogin
api-version: "1.20"
author: Xanemi-Dev
description: Central login plugin for your server — replace with a fuller description.
commands:
  login:
    description: Log in to the central authentication system.
    usage: /login <password>
    permission: centrallogin.command.login
permissions:
  centrallogin.command.login:
    description: Allows use of /login
    default: true
